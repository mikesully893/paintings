setup:
  addons:
  - plan: heroku-postgresql
  config:
    S3_BUCKET: django-static-paintings-1001
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - python manage.py collectstatic --noinput
run:
  web: gunicorn paintingstore_project.wsgi